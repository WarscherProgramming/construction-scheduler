import EmptyState from "../EmptyState";


function DashboardEmptyState() {
  return (
    <EmptyState
      title="Select a project to view its dashboard"
      description="Choose a project from Home to load its current summary."
    />
  );
}

export default DashboardEmptyState;
